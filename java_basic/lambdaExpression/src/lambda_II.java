/*
    Stream:

    i. Intermediate operations

    ii. Terminate Operations- return a result

 */

import java.util.ArrayList;
import java.util.List;
import java.util.stream.Collectors;

public class lambda_II {

//    List<Employee> employees = new ArrayList();
//    employees.stream().filter(em -> employee.getAge() >= 50).collect(Collectors.toList());
    List<List<Integer>> a;

    //el > 5;
    List<Integer> res = new ArrayList<>();
//    a.stream().forEach(list -> {
//                List<Integer> tmp = list.stream().
//                filter(x -> x > 5).collect(Collectors.toList());
//                res.addAll(tmp);
//    });
// Map, flatMap: 拼接过程， 把所有的东西都stream了
// ls.streams().flatmap(l->l.streams()).filter(e - > e> 5).collect(Collectors.toList());


}



