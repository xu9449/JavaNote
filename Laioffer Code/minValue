public class minValue {
	public int check(int arr[]) {
		//int result = -10000;	
		int max = Integer.MIN_VALUE;
		//for (int i = 0; i < arr.length; i++) {
		for ( int i : arr) {	
			max = (max > i) ? max : i;
			//if ( arr[i] > result ) {
			//	result = arr[i];
			}
		return max;
	
			}
	
	
}

